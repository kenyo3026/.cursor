---
alwaysApply: false
description: Release note generation standards and format
---
# Release Note Generation Rules

## Release Note Format and Structure

When generating release notes, follow this exact format and structure:

### Title Format
```markdown
# v{VERSION} - {EMOJI} {Main Feature or Improvement Title}
```

Example:
- `# v0.1.0 - 🚀 Initial Release`
- `# v0.2.0 - 🔧 Enhanced Configuration Management`

### Release Notes Section Structure

1. **Main Title** (H1):
   ```markdown
   # v{VERSION} - {EMOJI} {Title}
   ```

2. **Release Notes Section** (H2):
   ```markdown
   ## 📝 Release Notes
   ```

3. **Brief Description**:
   - One concise paragraph describing the release
   - Focus on the main purpose or achievement

4. **Core Features Section** (H3):
   ```markdown
   ### ✨ Core Features
   ```
   - Use bullet points with emoji and bold feature names
   - Format: `- **{EMOJI} {Feature Name}**: {Brief description}`
   - Each feature should be concise and user-focused
   - Group related features logically

5. **Technical Highlights Section** (H3):
   ```markdown
   ### 🔧 Technical Highlights
   ```
   - Focus on technical improvements and architecture
   - Use bullet points without emoji in feature names
   - Format: `- {Technical detail or improvement}`
   - Keep descriptions concise and technical

### Patch Version (X.Y.Z) Release Notes

For patch releases (e.g., v0.2.1, v0.3.1), keep the release note minimal:

1. **Title**: `# vX.Y.Z - 🐛 {Short Description}`
2. **Brief Description**: One sentence only
3. **Bug Fixes Section** only — no Core Features or Technical Highlights sections:
   ```markdown
   ### 🐛 Bug Fixes
   - **Fix** {description}
   ```
4. Omit Technical Highlights unless there is a non-trivial structural change

Example:
```markdown
# vX.Y.Z - 🐛 {Component} Bug Fixes

## 📝 Release Notes

Patch release addressing bugs found in vX.Y.0.

### 🐛 Bug Fixes

- **Fix** {serialization issue description}
- **Fix** {lookup / resolution issue description}
- **Fix** {import / dependency issue description}
```

### Content Guidelines

1. **Feature Descriptions**:
   - Keep descriptions concise and focused
   - Use present tense
   - Focus on user-facing benefits for Core Features
   - Focus on technical details for Technical Highlights
   - Avoid verbose explanations

2. **Emoji Usage**:
   - Use emojis in Core Features section for visual appeal
   - Common emojis: 🤖 (AI/Agent), 💾 (Storage), ⚙️ (Config), 🛠️ (Tools), 🌐 (Network), 🎨 (UI/Output), 📦 (Packages)
   - Do not use emojis in Technical Highlights section

3. **Language Style**:
   - Be concise and informative
   - Use professional but friendly tone
   - Avoid jargon unless necessary
   - Keep each bullet point to one line when possible

4. **Structure Principles**:
   - Keep release notes brief and scannable
   - Focus on what matters most to users
   - Avoid redundant information
   - Do not include Statistics, Quick Start, or Thank You sections unless specifically requested

### Generating Release Notes Process

1. **Get Commit History**:
   ```bash
   git log {previous_tag}..{current_tag} --pretty=format:"%h - %s (%an, %ar)" --reverse
   ```

2. **Get Statistics**:
   ```bash
   git diff {previous_tag}..{current_tag} --shortstat
   git diff {previous_tag}..{current_tag} --stat
   ```

3. **Analyze Commits**:
   - Group commits by type (FEAT, FIX, REFACTOR, CHORE, etc.)
   - Identify major features and improvements
   - Note breaking changes if any
   - Identify bug fixes

4. **Write Release Notes**:
   - Start with the most important features
   - Group related changes together
   - Use consistent formatting
   - Include all relevant improvements and fixes

### File Naming Convention

Release notes should be saved as:
- `release-note.md` (for current/latest release)
- Or `RELEASE_NOTES_{VERSION}.md` (for version-specific files)
- Example: `release-note.md`, `RELEASE_NOTES_v0.1.0.md`

### When User Requests Release Note Generation

1. Ask for or identify the version tag or commit hash
2. Run git commands to get commit history (optional, for reference)
3. Analyze commits and categorize them
4. Generate release note following the format above:
   - Start with H1 title: `# v{VERSION} - {EMOJI} {Title}`
   - Add H2: `## 📝 Release Notes`
   - Add brief description paragraph
   - Add `### ✨ Core Features` section
   - Add `### 🔧 Technical Highlights` section
5. Keep it concise - avoid verbose descriptions
6. Save to `release-note.md` or version-specific file
7. Review for grammar and consistency

### Example Template

```markdown
# v{VERSION} - {EMOJI} {Title}

## 📝 Release Notes

{Brief one-paragraph description of the release}

### ✨ Core Features

- **{EMOJI} {Feature Name}**: {Brief description}
- **{EMOJI} {Feature Name}**: {Brief description}

### 🔧 Technical Highlights

- {Technical improvement or detail}
- {Technical improvement or detail}
```

